name: Publish preview update

on:
  push:
    branches: ["*"]

jobs:
  publish_preview_update:
    name: Publish preview update
    type: update
    params:
      # This will use your git branch name as the EAS update branch
      branch: ${{ github.ref_name || 'test' }}
      platform: android
