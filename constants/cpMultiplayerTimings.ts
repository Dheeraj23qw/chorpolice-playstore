export const CP_MULTIPLAYER_TIMINGS = {
  /** Fast 4-card board shuffle for multiplayer dealing. */
  DEAL_SHUFFLE_DURATION_MS: 1400,

  /** Start fading non-public cards near the end of the short shuffle. */
  DEAL_NON_REVEAL_FADE_OFFSET_MS: 1050,
} as const;
